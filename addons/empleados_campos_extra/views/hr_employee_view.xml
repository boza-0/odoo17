<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <!-- Heredamos la vista formulario de empleados -->
    <record id="view_employee_form_inherit_campos_extra" model="ir.ui.view">
        <field name="name">hr.employee.form.campos.extra</field>
        <field name="model">hr.employee</field>
        <field name="inherit_id" ref="hr.view_employee_form"/>
        <field name="arch" type="xml">

            <!-- Insertamos los campos dentro del formulario -->
            <xpath expr="//sheet/group" position="inside">

                <group string="Información adicional">
                    <field name="edad"/>
                    <field name="fecha_revision_medica"/>
                    <field name="grupo_puesto"/>
                </group>

            </xpath>

        </field>
    </record>

</odoo>
